# ==============================================================================
# ACMP PRODUCTION Environment Variables
# ==============================================================================
# For AWS Secrets Manager or App Runner environment configuration
# Configured for 25,000 users
# ==============================================================================

# ------------------------------------------------------------------------------
# Application
# ------------------------------------------------------------------------------
NODE_ENV=production
PORT=3000

# ------------------------------------------------------------------------------
# Database (RDS PostgreSQL - db.t3.medium)
# ------------------------------------------------------------------------------
DATABASE_HOST=acmp-production.xxxxxxxxx.af-south-1.rds.amazonaws.com
DATABASE_PORT=5432
DATABASE_USER=acmp_admin
DATABASE_PASSWORD=<from-secrets-manager>
DATABASE_NAME=acmp_production
DATABASE_SSL=true

# Full connection URL format (alternative)
DATABASE_URL=postgresql://acmp_admin:<password>@acmp-production.xxxxxxxxx.af-south-1.rds.amazonaws.com:5432/acmp_production?sslmode=require

# ------------------------------------------------------------------------------
# Redis (ElastiCache - cache.t3.micro)
# Required for production: session storage, caching, rate limiting
# ------------------------------------------------------------------------------
REDIS_HOST=acmp-redis.xxxxxx.0001.afs1.cache.amazonaws.com
REDIS_PORT=6379
REDIS_URL=redis://acmp-redis.xxxxxx.0001.afs1.cache.amazonaws.com:6379

# ------------------------------------------------------------------------------
# JWT Authentication
# ------------------------------------------------------------------------------
JWT_SECRET=<from-secrets-manager>
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# ------------------------------------------------------------------------------
# Mailchimp Transactional (Mandrill)
# ------------------------------------------------------------------------------
MAILCHIMP_API_KEY=<from-secrets-manager>
MAILCHIMP_FROM_EMAIL=noreply@atfchallenge.org
MAILCHIMP_FROM_NAME=ATF AI Challenge

# ------------------------------------------------------------------------------
# AWS S3 (File Storage)
# ------------------------------------------------------------------------------
AWS_ACCESS_KEY_ID=<from-iam-role>
AWS_SECRET_ACCESS_KEY=<from-iam-role>
AWS_REGION=af-south-1
AWS_S3_BUCKET=acmp-uploads-prod

# S3 signed URL expiration (seconds)
AWS_S3_SIGNED_URL_EXPIRES=3600

# ------------------------------------------------------------------------------
# Google Gemini AI
# ------------------------------------------------------------------------------
GEMINI_API_KEY=<from-secrets-manager>

# ------------------------------------------------------------------------------
# CORS & URLs
# ------------------------------------------------------------------------------
CORS_ORIGIN=https://acmp.io,https://www.acmp.io
FRONTEND_URL=https://acmp.io
API_URL=https://api.acmp.io

# ------------------------------------------------------------------------------
# Logging (production-appropriate)
# ------------------------------------------------------------------------------
LOG_LEVEL=info

# ------------------------------------------------------------------------------
# Rate Limiting
# ------------------------------------------------------------------------------
THROTTLE_TTL=60
THROTTLE_LIMIT=100

# ------------------------------------------------------------------------------
# SMTP (Email) - Uses Mailchimp
# ------------------------------------------------------------------------------
SMTP_FROM_EMAIL=noreply@atfchallenge.org
SMTP_FROM_NAME=ATF AI Challenge

# ------------------------------------------------------------------------------
# Security
# ------------------------------------------------------------------------------
# Helmet and security headers are configured in code
# Session configuration
SESSION_SECRET=<from-secrets-manager>
