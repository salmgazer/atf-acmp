# ==============================================================================
# ACMP STAGING Environment Variables
# ==============================================================================
# For AWS Secrets Manager or App Runner environment configuration
# Cost-optimized for development/testing
# ==============================================================================

# ------------------------------------------------------------------------------
# Application
# ------------------------------------------------------------------------------
NODE_ENV=staging
PORT=3000

# ------------------------------------------------------------------------------
# Database (RDS PostgreSQL - db.t3.micro FREE TIER)
# ------------------------------------------------------------------------------
DATABASE_HOST=acmp-staging.xxxxxxxxx.af-south-1.rds.amazonaws.com
DATABASE_PORT=5432
DATABASE_USER=acmp_admin
DATABASE_PASSWORD=<from-secrets-manager>
DATABASE_NAME=acmp_staging
DATABASE_SSL=true

# Full connection URL format (alternative)
DATABASE_URL=postgresql://acmp_admin:<password>@acmp-staging.xxxxxxxxx.af-south-1.rds.amazonaws.com:5432/acmp_staging?sslmode=require

# ------------------------------------------------------------------------------
# Redis (SKIP for staging - use in-memory)
# ------------------------------------------------------------------------------
# REDIS_HOST=
# REDIS_PORT=
# REDIS_URL=

# ------------------------------------------------------------------------------
# JWT Authentication
# ------------------------------------------------------------------------------
JWT_SECRET=<generate-with: openssl rand -base64 64>
JWT_EXPIRES_IN=7d

# ------------------------------------------------------------------------------
# Mailchimp Transactional (Mandrill)
# ------------------------------------------------------------------------------
MAILCHIMP_API_KEY=<from-secrets-manager>
MAILCHIMP_FROM_EMAIL=noreply-staging@atfchallenge.org
MAILCHIMP_FROM_NAME=ATF AI Challenge (Staging)
MAILCHIMP_API_KEY=<from-secrets-manager>
MAILCHIMP_FROM_EMAIL=noreply-staging@atfchallenge.org
MAILCHIMP_FROM_NAME=ATF AI Challenge (Staging)

# ------------------------------------------------------------------------------
# AWS S3 (File Storage)
# ------------------------------------------------------------------------------
AWS_ACCESS_KEY_ID=<from-iam-role>
AWS_SECRET_ACCESS_KEY=<from-iam-role>
AWS_REGION=af-south-1
AWS_S3_BUCKET=acmp-uploads-staging

# ------------------------------------------------------------------------------
# Google Gemini AI
# ------------------------------------------------------------------------------
GEMINI_API_KEY=<from-secrets-manager>

# ------------------------------------------------------------------------------
# CORS & URLs
# ------------------------------------------------------------------------------
CORS_ORIGIN=https://staging.acmp.io
FRONTEND_URL=https://staging.acmp.io
API_URL=https://api-staging.acmp.io

# ------------------------------------------------------------------------------
# Logging (verbose for staging)
# ------------------------------------------------------------------------------
LOG_LEVEL=debug

# ------------------------------------------------------------------------------
# SMTP (Email) - Uses Mailchimp, SMTP optional for testing
# ------------------------------------------------------------------------------
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
SMTP_FROM_EMAIL=noreply-staging@atfchallenge.org
SMTP_FROM_NAME=ATF AI Challenge (Staging)
